<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>



<h2>Comments</h2>
<h1>El render va a iterar por cada article.comments</h1>
<%= render @article.comments %> 




<h2>Add a comment:</h2>
<h2>El form_with como recibe un array, crea una ruta unida, por lo tanto va a ser, /articles/:id/comments. me imagino que POST</h2>
<%= render 'comments/form' %>